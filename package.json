{
  "name": "toast-project",
  "version": "1.0.0",
  "scripts": {
    "build": "browserify src/main.js -t babelify --outfile lib/main.js"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0"
  }
}
